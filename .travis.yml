language: objective-c
rvm:
    - 2.4
env:
    - TMP_DIR=./tmp
before_install:
    - sudo rm -rf ~/.rubymotion && sudo motion update && motion repo
gemfile:
  - Gemfile
script:
    - bundle exec rake spec
